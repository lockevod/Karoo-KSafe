# Project exclude paths
/.gradle/
/app/build/
/app/build/intermediates/javac/debug/compileDebugJavaWithJavac/classes/
/app/build/intermediates/javac/debugAndroidTest/compileDebugAndroidTestJavaWithJavac/classes/

# Local machine configuration — contains SDK path and SECRET credentials (never commit)
local.properties

# Internal planning / design notes — kept local, not part of the public repo
/docs/superpowers/

# Crash-detection calibration ground-truth labels. Kept local only — Sergi's
# personal calibration data (rider reports + per-session FN/FP labels), not
# redistributed. Untracked 2026-05-31; the file stays on disk.
/docs/calibration-annotations.md

# macOS Finder cruft
.DS_Store
**/.DS_Store
/logs/
/logs/logs_sergi/

# Pulled Karoo system APKs + decompiled output used to explore the private
# AIDL surface (HAL buzzer bypass, sensorservice / activityservice). Kept
# locally for re-verification after Karoo OTAs; not redistributed.
/system_apks/
/decompiled/
/hal_dump.txt
/launcherapp.apk
